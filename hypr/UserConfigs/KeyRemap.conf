# Key Remapping
# Caps Lock → Escape
# Escape → Tilde (~)

# Apply using input settings
# input {
#     kb_options = caps:escape
# }

# Escape → Tilde: swap keys using keycode approach
# Using uinput to create a custom key remapper would be needed for full swap
# For now, we can simulate Escape→Tilde using bind with keycode
